def prblm(n):
    for i in range(n):
        print(i)
        

prblm(500)

#Linear time complexity O(n), the execution time grows proportionally with the input size.
#if we increase the input size, no. of operations will also increase.
